<?php

return [
    // write: main_page/header
    // read: /partneram
    'arGroups' => $arGroups,
    // write /catalog/index
    '_SESSION.per_page' => $_SESSION['per_page'],

    // catalog

    'iblock_id' => $iblock_id,
    'arItem' => $arItem,
];
